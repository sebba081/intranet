<!DOCTYPE html>
<html lang="es">
<%- include('partials/head') %>

<body class="login-modern light-mode" data-theme="light">
  <main class="login-shell">
    <section class="login-brand-panel">
      <div class="brand-badge">INSTITUTO</div>
      <h1>Intranet Académica</h1>
      <p>Accede a cursos, evaluaciones, asistencia, documentos y administración desde una plataforma moderna y minimalista.</p>
      <ul>
        <li><i class="fas fa-check-circle"></i> Panel personalizado por rol</li>
        <li><i class="fas fa-check-circle"></i> Comunicación y anuncios centralizados</li>
        <li><i class="fas fa-check-circle"></i> Modo claro/oscuro persistente</li>
      </ul>
    </section>

    <section class="login-card-wrap">
      <div class="login-card-modern">
        <div class="login-card-header">
          <div>
            <h2>Iniciar sesión</h2>
            <p>Ingresa con tus credenciales institucionales</p>
          </div>
          <button id="themeToggle" class="btn btn-outline-secondary btn-sm" type="button" aria-label="Cambiar tema">
            <i class="far fa-moon"></i>
          </button>
        </div>

        <% if (typeof error !== 'undefined' && error) { %>
          <div class="alert alert-danger" role="alert"><%= error %></div>
        <% } %>

        <form action="/login" method="POST" class="login-form-modern">
          <label for="email" class="form-label">Correo electrónico</label>
          <div class="input-group mb-3">
            <span class="input-group-text"><i class="fas fa-envelope"></i></span>
            <input id="email" type="email" name="email" class="form-control" placeholder="nombre@instituto.edu" required>
          </div>

          <label for="password" class="form-label">Contraseña</label>
          <div class="input-group mb-3">
            <span class="input-group-text"><i class="fas fa-lock"></i></span>
            <input id="password" type="password" name="password" class="form-control" placeholder="••••••••" required>
          </div>

          <div class="d-flex justify-content-between align-items-center mb-3">
            <div class="form-check">
              <input class="form-check-input" type="checkbox" id="remember" name="remember">
              <label class="form-check-label" for="remember">Recordarme</label>
            </div>
            <a href="/forgot-password" class="login-link">¿Olvidaste tu contraseña?</a>
          </div>

          <button type="submit" class="btn btn-success w-100 login-submit">Entrar a la intranet</button>
        </form>
      </div>
    </section>
  </main>

  <%- include('partials/scripts') %>
</body>
</html>
